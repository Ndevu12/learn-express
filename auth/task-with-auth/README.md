# Task API with authentication

Session 4 backend. See [../README.md](../README.md) for full setup, endpoints, and demo accounts.

```bash
cp .env.example .env   # optional
npm install
npm run dev            # http://localhost:4000
```
