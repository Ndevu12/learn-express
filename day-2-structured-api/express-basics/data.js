const obj = {
    hello: "kama",
    another: 3
};

let other = 4;

const { hello } = obj;

console.log(
    `Let us see what is inside another: ${obj.another}`
);

console.log(`Let us see what is inside Hello: ${hello}`)
