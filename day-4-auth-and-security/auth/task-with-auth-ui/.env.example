# API base URL (must match auth API CORS_ORIGIN when not using the proxy)
VITE_API_URL=http://localhost:4000

# Optional: use Vite proxy instead — set VITE_API_URL=/api in .env
# VITE_API_PROXY_TARGET=http://localhost:4000
